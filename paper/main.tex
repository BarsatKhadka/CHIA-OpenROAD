% CHIA-OpenROAD --- A3 CHIA Hackathon technical write-up (4 pages, ACM sigconf).
%
% Written bottom-up, one piece at a time: container -> node -> cluster ->
% boundary -> surrogate -> loop -> results. Context and contributions get
% written LAST, once we can see what the system actually turned out to be.
\documentclass[sigconf,nonacm]{acmart}

\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning}

\begin{document}



\title{CHIA-OpenROAD: An Agentic RTL-to-GDS Loop with a Replaceable
       Surrogate Evaluator}
\author{Barsat Khadka}
\affiliation{\institution{}\country{}}


\maketitle

% ---------------------------------------------------------------- LAST -----
% \section{Context and contributions}
\section{Context and Contributions}
CHIA is an open framework from UC Berkeley for building agentic
hardware/software co-design flows on a distributed Ray runtime~\cite{chia}. A
workflow is ordinary Python. A \emph{node} is a class whose methods, marked
\texttt{@ChiaFunction}, run on cluster workers, and a \texttt{ChiaTool} exposes
a chosen set of capabilities to an agent over MCP. Its published case studies
share the pattern of agent proposing and editing, and a trusted, AI-free
evaluation decides whether the result is accepted.

This paper contributes a surrogate interface with three unlike implementations
behind it, one a published clock-tree model, in which a model declares the
stage it observes and the artifacts it needs and the loop supplies file paths
rather than features. It contributes a trust boundary enforced in code rather
than in a prompt, under which no tool the agent can call runs sign-off, parses
a report, or invokes the flow. It contributes four measured causes of an
agentic tuning loop failing to improve across turns, each fixed and each
verified by the behaviour changing rather than the score alone. And it
contributes a set of failures that produce plausible numbers rather than
errors, which anyone building such a loop will meet.

\section{The loop}

\subsection{Where the flow runs}

The unit of execution is a single container image that is simultaneously a
complete OpenROAD-flow-scripts (ORFS) installation and a valid CHIA worker. It
is built on the upstream ORFS image and adds the Python environment CHIA
requires (Ray and \texttt{chia} on Python 3.10) so that a
\texttt{@ChiaFunction} body runs inside the container and invokes
\texttt{make} directly, rather than a host process shelling out to
\texttt{docker run}.
Building the image first forces the local and cluster execution models
to be identical from the start.

\textbf{Failure mode:} ORFS's helper scripts import \texttt{yaml} and
\texttt{pandas} from whatever \texttt{python3} resolves to, via
\texttt{PYTHON\_EXE ?= \$(shell command -v python3)}. Once CHIA's conda
environment is active, which is how the Ray worker starts, that resolves to
conda's interpreter, which does not have them. Pinning \texttt{PYTHON\_EXE} to
the system interpreter in the image keeps ORFS's scripts on the interpreter
they were built against while CHIA keeps its own.

\subsection{Running a stage}

Taking RTL to a GDSII is a fixed sequence of physical-design steps, and
ORFS packages each as a make target that reads the previous step's checkpoint
and writes its own (Figure~\ref{fig:stages}). A decision taken early constrains
everything after it and every step leaves a checkpoint, so a flow can resume
from any point rather than repeat. These stages are controlled through knobs:
make variables that ORFS's scripts read as they run. There are 249 of them in
the version used here (OpenROAD \texttt{26Q3-1817}), of which ORFS marks 18 as
tunable, and each is read at a particular step, so changing one invalidates
that step and every step after it. The flow is wrapped as a node, \texttt{OpenROADNode}. Its \texttt{run\_stage}
member takes a work directory, a design configuration and a knob mapping
to invoke the corresponding make target inside the container which returns the
checkpoint written, the elapsed wall-clock time, and the metrics ORFS records
in its per-stage JSON. No value is recovered by parsing log text.
A knob reaches the flow as \texttt{KNOB\_NAME=VALUE} appended to that make
command. Every knob is therefore checked against a small set of rules before
invocation. A name must be upper-snake-case, since it is placed in an
\texttt{argv} where an unconstrained key could introduce an additional flag or
make target,  and it must appear among the 249 known variables
(extracted from ORFS's own \texttt{variables.json} into a generated
\texttt{knob\_specs} module), so a misspelling never reaches the flow.

\textbf{Failure mode:} \texttt{make lvs} exits 0 whether or not the netlists
match. The \texttt{sky130hd} deck has its \texttt{raise} commented out and
merely prints the error, so the return code certifies a layout that has failed
LVS as clean. So , the verdict of lvs must be read from the report and not the exit status.

\subsection{Invalidation}

While changing a knob must invalidate the step that reads it and every step
after, the build system does not implement this. Make decides staleness by
comparing file timestamps, and a knob is not a file. This was confirmed with
\texttt{make -n}, which reports that everything is up to date after
\texttt{CORE\_UTILIZATION} (floorplan) is changed, after
\texttt{CTS\_CLUSTER\_SIZE} (clock-tree synthesis) is changed, and after
setting a variable that does not exist at all. A search loop built directly on
this would request a new configuration and receive the previous design in its
place, with nothing in the result to indicate the substitution.

Invalidation is therefore performed explicitly. Each work directory carries a
manifest recording the knobs that produced its checkpoints. Before a stage is
invoked, the requested knobs are compared against that manifest, the earliest
step at which any differing knob is read is determined from the extracted
variable table, and every checkpoint from that step onward is deleted. A change
confined to clock-tree knobs therefore preserves synthesis, floorplanning and
placement, while a change to core utilisation discards everything from
floorplanning on.
\subsection{On a cluster}

Evaluating one configuration means running a design through the entire flow
(Figure~\ref{fig:stages}), and the number of admissible configurations grows
combinatorially with the number of knobs, so exhaustive evaluation is
infeasible. The knobs also interact, for example placement density must exceed core
utilisation or placement cannot legalise, and the effect of a clock-tree knob
depends on the floorplan beneath it. Design space exploration therefore reduces
to choosing few configurations carefully and evaluating those few in parallel. 
Ray has no notion of ORFS, so the cluster declares a custom resource,
\texttt{orfs}, with a count, and each candidate reserves one unit of it for the
duration of its build. Once the units are taken, further candidates queue
rather than oversubscribe the machine. The thread count handed to each build
follows from the same number, namely the machine's cores divided by the
declared unit count. The members of a node cannot be scheduled independently. \texttt{run\_stage}
writes a work directory on a worker's local disk, and the members that later
read metrics or measure the clock tree operate on that same directory, so all
\begin{figure*}[t]\centering
\begin{tikzpicture}[
  font=\small,
  stage/.style={draw,rounded corners=1pt,minimum height=7mm,minimum width=15mm,
                align=center,inner sep=2pt},
  ck/.style={font=\scriptsize\ttfamily,text=black!60},
  t/.style={font=\scriptsize},
  ar/.style={-{Latex[length=1.6mm]},shorten >=1pt,shorten <=1pt},
]
\def\stagesep{6mm}
\node[stage] (syn) {synth};
\node[stage,right=\stagesep of syn] (fp) {floorplan};
\node[stage,right=\stagesep of fp]  (pl) {place};
\node[stage,right=\stagesep of pl]  (ct) {cts};
\node[stage,right=\stagesep of ct]  (gr) {global\\route};
\node[stage,right=\stagesep of gr,fill=black!12] (dr) {detailed\\route};
\node[stage,right=\stagesep of dr]  (fi) {finish};

\foreach \a/\b in {syn/fp,fp/pl,pl/ct,ct/gr,gr/dr,dr/fi}
  \draw[ar] (\a) -- (\b);

\foreach \n/\c in {syn/1\_synth.odb, fp/2\_floorplan.odb, pl/3\_place.odb,
                   ct/4\_cts.odb, gr/5\_1\_grt.odb, dr/5\_route.odb,
                   fi/6\_final.gds}
  \node[ck,below=1.2mm of \n] {\c};

\foreach \n/\s in {syn/51, fp/3, pl/44, ct/23, gr/270, dr/1306, fi/24}
  \node[t,above=1.0mm of \n] {\s\,s};

\node[t,anchor=west,text=black!60] at ([yshift=-9mm]syn.south west)
  {A knob first read at stage $k$ invalidates every checkpoint from $k$ onward.};
\end{tikzpicture}
\caption{The ORFS stage chain, with the checkpoint each stage leaves and its
measured cost on aes/sky130hd at 8 threads (1721\,s total). Detailed routing is
76\% of the flow, so any knob change that reaches it costs close to a full
build.}
\label{fig:stages}
\end{figure*}
of them must execute on the worker that holds it. Declaring the node as a
\texttt{ColocatedNode} places its members in a single placement group and
satisfies this.


\section{The trust boundary}

The agent participates by proposing configurations, not by executing anything.
Each iteration renders the ledger of what has been built, what it was derived
from, its measured metrics, and what failed and why, as text. That text is the
entire prompt. The agent replies with configurations, the driver validates each
one and dispatches the survivors, and the results are written back to the
ledger. The agent holds no state between iterations, so its memory is the
ledger and nothing else.


Within a turn the agent may call the read-only tools listed in
Table~\ref{tab:tools}. All of them are database queries or fitted-model
evaluations and complete in milliseconds. Proposing a configuration is not
among them, because it is not a tool call at all. The agent's reply is text,
and the driver parses the configurations out of it, validates each one, and
dispatches the builds itself. The set of tools is not fixed. A tool is a method
on a \texttt{ChiaTool}, so adding one is adding a method, and the boundary is
preserved by what a tool is permitted to do rather than by keeping the list
short.
\begin{table}[t]\footnotesize\centering
\caption{The tools available to the agent within a turn. Every one is
read-only and completes in milliseconds. The set is extensible; the constraint
is on what a tool may do, not on how many exist.}
\label{tab:tools}
\begin{tabular}{@{}lp{4.5cm}@{}}
\toprule
Tool & Returns \\
\midrule
\texttt{list\_legal\_knobs}   & the settable knobs, with stage and range \\
\texttt{past\_failures}       & configurations known not to build, and why \\
\texttt{list\_candidates}     & every configuration evaluated so far \\
\texttt{compare\_candidates}  & those configurations ranked by a chosen metric \\
\texttt{best\_candidate}      & the best configuration by a chosen metric \\
\texttt{screen\_candidates}   & a precomputed surrogate ranking over a grid \\
\texttt{describe\_surrogates} & what each fast model reads and predicts \\
\texttt{predict\_knobs}       & predictions for configurations the agent supplies \\
\bottomrule
\end{tabular}
\end{table}
This is where the pattern from CHIA's case studies is enforced. The agent never
runs the flow. It names a configuration and the driver runs it, so every number
in the ledger is one ORFS reported rather than one the agent supplied. It
cannot run DRC or LVS, override knob validation, or force a rebuild that would
bypass invalidation. Whether a configuration builds at all is decided by ORFS,
and when one fails the recorded reason is the tool's own message, for example
that global routing finished with congestion.

\textbf{Failure mode:} what matters is not whether the agent may call tools but
how long those calls take. An earlier design gave the agent the build as a tool
and held one model session open across it. On two occasions the session ignored
its timeout and hung for over two hours while the build completed underneath it
and went uncollected. Five successive runs failed this way, none for reasons
connected to the agent's reasoning. Hence , in this design fast tools belong inside the turn but the
build belongs outside it.
\section{The surrogate socket}

Running a stage is expensive, so surrogate models have started being widely
used in the flow.

Every surrogate predicts from features of a completed stage, but which features
differ from model to model. We therefore do not compute features. A model
declares the stage it observes and the files it needs, the loop produces those
files, and the author writes the parsing inside the model.
Figure~\ref{fig:socket} shows the form. The model returns one prediction per
configuration. The agent invokes a model and gets a prediction back. Each iteration it also
names a shortlist wider than it can afford to build, and the models price that
shortlist before it commits. A grid of configurations is scored once in
advance, so the agent can read off the best predicted combinations without
asking for them. A prediction is trusted only inside the range the model was fitted on, so
configurations outside that range are never sent to it.
\begin{figure}[t]
\begin{small}
\begin{verbatim}
class MyModel(SurrogateEvaluator):
    observes_stage = "place"
    requires       = ("def", "timing_rpt")
    predicts       = {"clock_skew_setup": "orfs_metric"}
    domain         = {"CTS_CLUSTER_SIZE": (12, 30)}

    def _predict(self, state, candidates):
        d = parse(state.artifacts["def"])
        ...
\end{verbatim}
\end{small}
\caption{A surrogate declares the stage it observes and the artifacts it
requires. The loop produces those artifacts and passes their paths; parsing and
feature extraction happen inside the model.}
\label{fig:socket}
\end{figure}

\textbf{Failure mode:} a fitted model cannot cross into the tool's MCP actor.
The tool is serialised when that actor is constructed and the actor has no
import path for a plugin's dependencies, so the run fails at startup with a
missing-module error that names neither the tool nor the model. The driver's
copy holds the models and answers prediction requests; the actor's copy holds
none and says so.



\section{Experimental setup}

Four designs are taken from CTS-Bench and ported into ORFS on the
\texttt{sky130hd} open PDK, keeping the clock ports and top modules the
benchmark uses. Each is given a clock period at which the shipped
configuration lands close to timing closure, so the search has room to work.
The periods are 6.5\,ns for \texttt{aes}, 4.5\,ns for \texttt{picorv32},
10.4\,ns for \texttt{sha256} and 6.5\,ns for \texttt{ethmac}.

Only \texttt{sha256} needed a period looser than its benchmark value, and the
reason is worth stating. At 10\,ns it sits 0.58\,ns from closure, its critical
path is logic-dominated, and no placement or clock-tree knob reaches it. Across
89 builds spanning five versions of the loop, three configurations beat the
default and by at most 0.044\,ns. At 10.4\,ns the same loop beats the default
in its first turn. A design far from closure reports a flat response to every
knob, and reading that as a failure of the search would be a mistake.

Every run measures its own default rather than importing one, so the baseline
is the configuration that run was optimising against. Runs are compared at
equal budget. All experiments use a single 32-vCPU instance; the project is
capped at 32 vCPUs globally, so throughput comes from evaluating candidates
concurrently rather than from larger machines.

\section{Results}
\label{sec:results}

\subsection{Does any of it help?}

\begin{table*}[!t]\scriptsize\centering
\caption{Four arms on four designs, twelve builds each. \emph{best} is the best
worst-slack reached, \emph{med} the median across that arm's builds,
\emph{beat} how many beat the default. \emph{preCTS} counts builds
that varied a knob read before clock-tree synthesis; it is zero for the screen
arm, whose ranking covers clock-tree knobs only. Slack in ns, power in W, area
in $\mu$m$^2$.}
\label{tab:arms}
\begin{tabular}{@{}llrrrrrr@{}}
\toprule
design & arm & best & med & power & area & beat & preCTS \\
\midrule
\texttt{aes}      & default & $-0.2755$ & --        & 0.0427 & 2.53e5 & --    & --    \\
                  & screen  & $-0.1478$ & $-0.2304$ & 0.0420 & 2.51e5 & 10/12 & 0/12  \\
                  & agent   & $-0.1651$ & $-0.2621$ & 0.0427 & 2.53e5 & 6/11  & 10/11 \\
                  & full    & $-0.1618$ & $-0.1987$ & 0.0390 & 2.47e5 & 7/10  & 9/10  \\
\midrule
\texttt{picorv32} & default & $-0.2352$ & --        & 0.0354 & 1.03e5 & --    & --    \\
                  & screen  & $-0.1660$ & $-0.2643$ & 0.0333 & 1.02e5 & 2/12  & 0/12  \\
                  & agent   & $-0.1772$ & $-0.2645$ & 0.0343 & 1.03e5 & 1/11  & 9/11  \\
                  & full    & $-0.2878$ & $-0.3723$ & 0.0347 & 1.03e5 & 0/12  & 12/12 \\
\midrule
\texttt{sha256}   & default & $-0.5778$ & --        & 0.0161 & 1.25e5 & --    & --    \\
                  & screen  & $-0.6018$ & $-0.6381$ & 0.0148 & 1.24e5 & 0/12  & 0/12  \\
                  & agent   & $-0.5471$ & $-0.7001$ & 0.0157 & 1.37e5 & 1/10  & 9/10  \\
                  & full    & $-0.5342$ & $-0.7125$ & 0.0216 & 1.30e5 & 1/12  & 11/12 \\
\midrule
\texttt{ethmac}   & default & $+0.1106$ & --        & 0.1505 & 4.82e5 & --    & --    \\
                  & screen  & $+0.1396$ & $+0.0659$ & 0.1322 & 4.68e5 & 2/12  & 0/12  \\
                  & agent   & $+0.3169$ & $+0.1337$ & 0.1837 & 5.12e5 & 7/12  & 10/12 \\
                  & full    & $+0.3703$ & $+0.2437$ & 0.1319 & 4.67e5 & 9/12  & 12/12 \\
\bottomrule
\end{tabular}
\end{table*}

Four arms are compared. \emph{Default} builds the shipped configuration once.
\emph{Screen} builds the surrogate's top-ranked configurations with no LLM.
\emph{Agent} runs the loop with the surrogate withheld. \emph{Full} is the
whole loop. Each search arm gets twelve builds.

The answer is that it depends on the design, sharply
(Table~\ref{tab:arms}). On \texttt{ethmac} the full loop is best on every
metric at once. On \texttt{aes} the surrogate ranking alone reaches the best
slack while the full loop spends 8.5\% less power and 2.4\% less area for 15\%
worse skew. On \texttt{sha256} the surrogate ranking fails outright with no
build beating the default, though both LLM arms beat it. And on
\texttt{picorv32} nothing works at all: every arm's median is worse than simply
building the shipped configuration.

Two statistics are reported because they disagree. Ranking arms by their best
single result rewards variance rather than quality, and \texttt{picorv32} shows
why: the screen arm holds the best single candidate while only two of its
twelve beat the default. On \texttt{aes} the disagreement runs the other way,
the full loop holding the best median while placing second on best-of-twelve.

\subsection{A loop that was not learning}

That table hides something worse than an inconsistent winner. Measured over
three turns on all four designs, the best result of each turn was flat or
oscillating in seven of eight runs, and the third turn was frequently worse
than the second. The loop was sampling, not searching, and a table of final
scores cannot show that.

\subsection{Four reasons, none of them the model}

Every cause turned out to lie in what the agent was shown rather than in its
reasoning. The prompt required each candidate to differ from everything already
built, which makes refining a good configuration illegal and restarts the
search every turn. A candidate derived from an earlier one did not inherit its
parent's knobs, so ``take \#6 and change one knob'' produced a near-default
build with a single knob set, and every refinement was silently a fresh sample.
The ledger never showed the default the loop was meant to beat, so a run where
all twelve candidates lost looked identical to one where all twelve won.
Finally the agent carried priors from other designs: lowering core utilisation
helps \texttt{aes} and \texttt{picorv32}, so it proposed that on
\texttt{sha256}, where pooling every build of that design shows utilisation
$45$ averaging $-0.675$ against $30$ at $-0.864$, and a setup margin of $0.0$
averaging $-0.682$ against $0.1$ at $-0.778$. It could not see that from twelve
rows with six to eight knobs varying at once, so the loop now computes the
aggregate per-knob effect from its own ledger and shows it, noting that the
evidence is design-specific.

Fixing all four changed the behaviour rather than only the score. On
\texttt{picorv32} the per-turn best went from $-0.2878$, $-0.3149$, $-0.3459$,
worse every turn, to $-0.2801$, $-0.2455$, $-0.2351$, better every turn, with
the median improving by 0.095\,ns. That the inheritance fix landed is visible
directly: the mean knobs per candidate rose from 3.0--3.8 to 5.5--7.3.

\subsection{One design still lost, and the clock was why}

\texttt{sha256} kept failing even after all four fixes. Across 89 builds
spanning five versions of the loop, three configurations beat its default and
by at most 0.044\,ns. The cause was not the search. At its benchmark period of
10\,ns the design sits 0.58\,ns from closure with a logic-dominated critical
path that no placement or clock-tree knob reaches, so every knob reports a flat
response. Given 10.4\,ns the same loop beats the default in its first turn.
A design far from closure will look identical to a search that does not work,
and distinguishing the two matters.

\subsection{Paying less for the same search}

\begin{table}[!t]\footnotesize\centering
\caption{The full loop after the four changes of \S5.2, evaluated
multi-fidelity: 24 configurations scored at clock-tree synthesis, the best
eight routed to GDS. \texttt{sha256} is measured at 10.4\,ns.}
\label{tab:final}
\begin{tabular}{@{}lrrrr@{}}
\toprule
design & default & best & $\Delta$ & beat \\
\midrule
\texttt{aes}      & $-0.2755$ & $-0.1651$ & $+0.1104$ & 5/6 \\
\texttt{picorv32} & $-0.2352$ & $-0.1874$ & $+0.0478$ & 6/8 \\
\texttt{sha256}   & $-0.3921$ & $-0.1513$ & $+0.2408$ & 7/8 \\
\texttt{ethmac}   & $+0.1106$ & $+0.4131$ & $+0.3025$ & 8/8 \\
\bottomrule
\end{tabular}
\end{table}

The remaining cost is that every candidate is routed. Scoring at clock-tree
synthesis instead averaged 1.0\,min against 22.9\,min for a full build on
\texttt{picorv32}, about 4\%, because detailed routing is roughly three
quarters of a flow. Exploring 24 configurations and routing the best eight cost
363\,min on \texttt{sha256} against an estimated 1007\,min had all 24 been
routed, and 215\,min against 566\,min on \texttt{picorv32}. The saving is
smaller than the per-build ratio implies because promoted builds still pay
full price.

It is a trade rather than a free gain. On \texttt{picorv32} routing everything
reached $-0.1482$ while routing eight of 24 reached $-0.1874$: quick-stage
slack ranks candidates at a Spearman correlation of $+0.77$ against final
slack, not perfectly, so a good configuration can go unpromoted.

Table~\ref{tab:final} reports where this leaves the loop. All four designs
improve on their defaults, and on three of them nearly every routed candidate
does. \texttt{sha256} is the clearest: the design that produced three wins in
89 builds now produces seven in eight.

\section{Limitations}

Every cell is a single run. Two configurations that allocate their budget
identically differed by 0.057\,ns in median on the same design, so that is the
noise floor, and differences smaller than it are not claimable. Seeds are the
obvious remedy and were not affordable within the compute budget.

The screen arm is not a clean control, as \S5.1 notes: it varies both the LLM
and the search space. An arm restricting the agent to clock-tree knobs would
isolate the LLM as a single variable.

The surrogate is used for one of the three quantities it declares. On these
designs it tracks clock wirelength to 2.6--3.1\% mean absolute error, but ORFS
produces no SAIF, so its power prediction has rank correlation 0.00 and is
unusable, and clock skew is not produced at all. An arm labelled ``with a
surrogate'' that screens one of three declared objectives should say so.

Sign-off is partial. DRC is clean; LVS is blocked by ORFS's own collateral,
where \texttt{sky130hd.cdl} is in a dialect the packaged KLayout cannot read
and the netlists do not match once that is worked around.



% ---------------------------------------------------------------- NEXT -----
% \subsection{Running a stage}   -- OpenROADNode: knobs in, metrics out
% \subsection{Invalidation}      -- the knob manifest
% \subsection{On a cluster}      -- the orfs resource, colocation
% \section{The trust boundary}
% \section{The surrogate socket}
% \section{Results}
% \section{What broke}
% \section{Limitations}

\end{document}
